# Project

AI-powered data analysis and visualization automation, built with the Agno agent framework (Python) and a Streamlit UI.

# Stack

- Python, Agno (agents), Streamlit (UI), UV (depency management)
- See `docs/requirements.md` for full project scope and requirements
- See `docs/plan.md` for the current phase's plan

# Workflow

Plan → Test → Build, one phase at a time.
- Plan: read `docs/requirements.md` and write/update `docs/plan.md` for the next phase only, before building
- Build: implement only that phase
- Test: write and run tests before moving to the next phase
- End of each phase: update "Current phase" (in 1 line short) below before starting the next

# Current phase

<!-- update this line each time a phase is completed -->
 phase 26 — Transform Data gains the nine clean-up steps it was missing (skip rows, remove empty rows, fix numbers stored as text, round, trim spaces, remove special characters, letter case, find & replace, unpivot), and each uploaded table gets a Fix headers button that opens Skip rows pre-filled; phase 27 (done) — Transform Data steps can be saved as a named pipeline that matches next month's upload by file name, checks the columns its steps actually need before running, replays every step with no AI, and hands the finished table to Chat with Data via an Export to menu; phase 28 (done) — Transform Data gains a Describe a step button beside Add a step: type an instruction in plain English, the Light Model turns it into steps from the existing catalog only (never code), you see them described and previewed before adding, and saved pipelines still replay with no AI; the model is sent each table's columns with their types, and answers in strict-schema-safe name/value pairs so cloud providers fill them in; an "update column X if Y" instruction is built as add/drop/rename so it actually overwrites the column, and its "otherwise" side keeps the column's real value (text included) instead of blanking it; phase 29 (done) — the calculated-column formula box can now test and choose: `amount > 1000` gives a 1/0 flag and `price * 0.9 if quantity > 100 else price` picks between two numbers (tests `> < >= <= = !=`, choices chainable, a blank or unreadable test takes the else side); it stays a printable whitelist with no functions and no text answers, so `add_conditional_column` is still the tool for text branches and the non-maths tests, and the AI is told to use the one-step formula only when both answers are numbers; phase 30 (done) — seven finance helpers join the catalog (36 operations now) with no UI change: Today's date, Add or subtract days, Start or end of month, Take part of a code (characters by position, for codes with no separator), Percentage of total, Drop the minus sign, and Smallest or largest across columns (sideways, per row — not a groupby); blanks rather than errors for unreadable values and a zero total, and all seven reach Describe a step straight from the registry; phase 31 (done) — development/testing and live data are now separate folders: every database and file-storage path goes through `utils/env.py::get_data_dir()`, which returns `data/` only when the setting `APP_ENV` is `"production"` (Streamlit Cloud *Settings -> Secrets*) and `data_testing/` otherwise, so an unconfigured laptop can never write to real data; `data_testing/` is gitignored and no UI changed; phase 32 (done) — Report Builder gains a **Dashboard** tab that builds an interactive HTML dashboard: an editable spec table (the add/delete-users pattern, not `st.data_editor`) of filters, cards, charts (bar, horizontal bar, line, area, pie, scatter) and flat tables, laid out by row number with filters on top or left; the data is flattened once in DuckDB from the relationships already confirmed in Setup (walking `Stock -> SubCategory -> Category` so a hierarchy filter is just a column test), embedded as columns+rows, and drawn by **Vega-Lite** vendored under `live_dashboard/assets/vendor/` (~0.8 MB, not Plotly's 4.9 MB) so grouping, totalling and click-to-cross-filter all happen in the browser with no server; the Streamlit preview and the download are the same string; the spec is saved inside the Task; a visual naming a column no confirmed link reaches is listed with a warning rather than silently joined on a guess; AI generation, templates, drill-down, pivot/map and the Chat with Data entry are phase 33+; phase 33 (done) - the Dashboard tab gains a **Describe a dashboard** button: type the page you want in plain English and the Light Model drafts the spec rows (`live_dashboard/ai_spec.py`), choosing only from the visual types, sub-types and aggregations the page already has and only from columns the flattened data really carries - an invented visual type, an unknown way of totalling, a merely similar column name or a text column asked to be summed is dropped with a sentence rather than drawn; a collapsed **Extra guidance for AI** box holds the user's own standing preferences ("always show currency in INR"), appended to the prompt below the fixed rules and saved inside the Task as `DashboardSpec.ai_guidance`; the draft is shown in words with **Replace the dashboard** or **Add to it**, and row numbers are laid out here when the model puts everything on row 1; once accepted the rows are ordinary rows - previewing, redrawing and downloading never call a model; phase 34 (done) - the dashboard's vocabulary widens so the coming chat mode has something to draw with: 14 ways to total a number (median, count unique, standard deviation, quartiles, first/last, percentage of total, running total) and 13 chart shapes (stacked and grouped bars, donut, histogram, heatmap, box plot, combo), with `live_dashboard/model.py` declaring its own `DASHBOARD_AGGREGATIONS` and `DASHBOARD_CHART_LABELS` rather than changing the five `analyst/charts.py` shares with Chat with Data; a shape we can't draw now falls back to the nearest one we can **and says so** ("asked for a treemap ... so it's a pie instead") instead of dropping the visual silently, while one with no honest stand-in (a map) still refuses; percentage-of-total and running-total are Vega-Lite transforms rather than aggregates, each refused where it would be meaningless (a percentage of nothing on a card, a running total over unordered labels); `format:currency` finally prints a symbol - a whitelisted `currency:INR` through `Intl.NumberFormat`, so the UI's own "always show currency in INR" example works; no change to how the page is used, and `tests/test_live_dashboard_runtime.py` lifts the card maths out of `runtime.js` and runs it in Node rather than grepping its source; phase 35 (done) - the Dashboard tab becomes a conversation: the Describe dialog and the Add a visual button are gone, replaced by a box on the page whose every press is one **round** - `ai_spec.revise_dashboard` shows the Light Model the page as it stands (`describe_spec_for_prompt`, numbered and field by field, the model's only memory since `run_structured` is stateless) and reads back *edits* (`action` add/update/remove with a `target` number), so a visual no proposal names is untouched and an updated one keeps its `panel_id`; a written design skill (`_DESIGN_RULES`, tied to the catalog by a test) rides with every request so pages come back with cards on row 1, two charts to a row and titles that name the number; the Setup data dictionary finally reaches the prompt via `column_notes`, matched onto flattened names like `Customer - CustName`; one-step **Undo** restores the spec JSON stashed before the last round, and a round that changed nothing throws the stash away; the spec table is now a read-only glance with Edit / Duplicate / Remove kept as the by-hand escape hatch; phase 36 (done) - the Dashboard tab keeps only one way to change a dashboard: the spec table, the nine-field edit form and the Add / Edit / Duplicate / Remove buttons are gone, so every change is a round, and the `Status` column's job is done by a warning above the preview naming any visual that can't be drawn; the **Extra guidance for AI** box goes too (a preference is a sentence, and the box above already takes sentences), taking `ai_guidance` off the spec while an older Task still opens; and the chart bug behind it all is fixed - `_top_n_transform` began with a Vega-Lite `aggregate`, which *replaces* its input with the group keys and totals, so the measure column stopped existing and a Top N chart drew a labelled axis and **no bars**; it now uses `joinaggregate` plus `dense_rank`, `_transform_aggregation` groups by the colour column as well (a stacked percentage silently drew in one colour with a "null" legend), and `tests/test_live_dashboard_vega_render.py` runs the **vendored** Vega in Node so a spec that is valid and draws nothing fails in pytest rather than in a reader's inbox; phase 37 (done) - the dashboard is easier to drive: filters now default to the left and open only when clicked, panel titles are centred, each filter has its own **Clear** beside Clear all, the instruction box moved into a dialog behind the **Update the dashboard** button, every visual gained an **Edit** button opening that same dialog scoped to it (`revise_dashboard(focus=...)` points every edit at that panel, so "make it horizontal" can't reshape another chart), the Dashboard is now a fourth view inside **Chat with Data** on the same spec rather than a second copy, and Undo finally drops the round it undid from "What was asked, and what changed" instead of leaving the history describing a change that is no longer on the page; phase 38 (done) - the dashboard stops failing quietly and gains the settings people actually asked for: an update that would leave a visual exactly as it was is no longer counted as a change (a pie asked to "show data labels" had no such setting, so the model re-sent the same fields and the round still said "Changed 1 ..."), and the note points at a new **What can I ask?** expander in both dialogs, built by `live_dashboard/help.py` from the very constants the AI catalog is rendered from so it cannot go stale; **Change this visual** gains a two-press **Remove this visual** that asks no model and that Undo reverses; seven look settings join the whitelist (`labels`, `legend`, `axis_titles`, `colour`, `size`, `width`, `card_size`), each a named choice `clean_properties` checks and `describe_properties_for_prompt` offers, so a setting can never be advertised in the prompt and dropped on arrival - data labels are a second layer of text marks since Vega-Lite has no switch for them, and on a pie they are the slice's percentage of the whole, which needs the slices totalled in the data first; a setting a shape cannot use (labels on a heatmap) is now *reported* rather than silently ignored; and `measure_column_2` finally becomes the list its own docstring asked for - `extra_measures` of `{column, aggregation, axis}`, so one chart can show the minimum, average and maximum of a column on one shared axis (compared by height, which is the point) or salary as bars against headcount as a line on a second axis, with one `aggregate` totalling every measure and each layer folding only the ones it draws so the layers cannot disagree about which categories exist, the measure axis split only when something is really drawn on the right, and an older saved dashboard migrated on load so it opens showing what it always showed; phase 39 (done) - filters finally reach every related table: `_build_data` flattens **every** table on its own with the parents it can reach (`flatten.flatten_every_table`), so Employee Master's two unrelated children Salary and Attendance each carry `Employee Master - Department` and one filter narrows both - a parent also repeats its own columns under that same prefixed name so the filter reaches the parent too, and `matchesGlobal(record, tableName)` now **skips** a rule whose column a table hasn't got instead of failing every row (which used to empty that panel outright); no table is ever joined to a sibling, so no row count can multiply; the Main table picker is gone and `DashboardSpec.main_table` is kept only so an older Task opens, with a visual that names no table going to the table that owns its columns; a number filter gains a second handle ("30,000 to 60,000", each handle pushing rather than passing the other); the single Undo button is replaced by one **Undo this** per round in "What was asked, and what changed" - each `Round` carries the dashboard from before it and only the newest is live, so the second round of an afternoon no longer throws away the only copy that could take you back; and a new primary **Generate Dashboard** button designs a whole page from the data using the session's own model (`llm_session.active_profile`) while every round stays on the Light Model, asking "this replaces everything" in two presses when the page already has visuals and putting the old panels back if the model answers with nothing; phase 40 (done) - a round is no longer stuck on the Light Model: a **toggle** above **Update the dashboard** switches it to the session's own model for the change that needs judgement rather than speed, off by default and moving both dialogs together (the per-visual Edit box is the same round with "which one do you mean" already answered), shown only where there is a second model to switch to and never saved into the Task - which model answered is a choice for this sitting; and a table gains a second style, the **drill-down**, which groups rows into collapsible layers with a total on each the way an Excel pivot table's row grouping does - it needed no new `PanelSpec` field and so no migration, reading `source_columns` as the ordered levels (two to four, outermost first) and `measure_column` + `aggregation` as the number totalled on every one, refusing `percent_of_total` and `running_total` because both are measured against a chart's other bars and a tree of groups has no single whole; the browser does the grouping with the `aggregate` the cards already use, the rows are built once and opened by CSS class rather than redrawn, a leaf click cross-filters on its own level, and the search box is left off because a drill-down shows totals per group rather than the rows a search would look through - so `tests/test_live_dashboard_runtime.py` runs the level totals in Node, where a pivot table that lies is caught in pytest rather than by the reader checking it by hand; phase 41 (done) - a drill-down table can total **several numbers**, one column each: `extra_measures` existed since phase 38 but was whitelisted to five chart shapes, so *"sum of cost, sum of quantity, and the average, minimum and maximum of price by category and item"* came back with the sum alone and four measures silently gone - the check moves out of the chart branch into `_extra_measure_problems`, a flat table is refused with a sentence of its own (it lists rows, so it has no totals to add a number to), the cap stays four in all and the `axis` a measure carries is ignored because columns have no sides; `drilldownGroups(rows, levels, measures)` now carries `values` per node and the renderer prints one heading per measure from labels Python built, with a count printed plain even on a currency panel; and the **Rows** column nobody asked for becomes a `row_count` setting - shown for a single total, hidden once there are several, overridable either way - so no new field is saved, no migration is needed and a page exported before this opens showing exactly what it always showed; phase 42 (done) - Transform Data gains **Combine columns** (Name + Surname with a space, dash, comma, slash, underscore, nothing or your own separator; blank pieces skipped so no dangling separators), and dates read dd-mm-yyyy everywhere through `utils/dates.py`: an Excel date cell loads as `03-04-2025` instead of `2025-04-03 00:00:00`, a date with no stated format is read ISO-first then day-first, every `st.dataframe` goes through `show_dataframe` (a display format, so sorting is still by date), Excel downloads keep real dates formatted `dd-mm-yyyy`, and CSV/HTML tables write them as text; the live dashboard and Streamlit's own toolbar download still use yyyy-mm-dd; phase 43 (done) - a dashboard bar chart can be sorted **by date** (`SORT_DATE`, ask "sort the bars by date"): month-year text like `Apr-2024` is read as a date inside the chart (`_sort_date`, kept through every aggregate) so the bars run oldest to newest instead of in data order, tested by drawing them in the vendored Vega; phase 44 (done) - three fixes: Transform Data's Column details says `numeric (stored as text)` for a column that only looks numeric (Round refused it) with one-click **Store as numbers** / **Store as dates** buttons that open the ordinary step pre-filled; dark-mode dashboard data labels are light instead of Vega's default black (`config.text.color`); and opening a Report Builder Task restores its "What the columns mean" descriptions, held until each table is loaded

# Library docs (Agno, Streamlit)

Agno and Streamlit APIs change frequently — do not rely on training knowledge for either.
- Streamlit: use the official `streamlit skills` install (auto-synced to installed version, no lookup needed)
- Agno: use the official docs MCP (`agno-docs`, https://docs.agno.com/mcp) for current API details.

After writing Agno/Streamlit code, verify it against these sources and flag anything deprecated.

# Coding conventions

- Naming: snake_case for functions/variables, PascalCase for classes, descriptive names (no `df1`, `temp`, `x`)
- Streamlit: every widget must have a unique `key=` and a `help=` tooltip
- streamlit: Use width property instead of old container_width property
- streamlit: show dates in table or dataframe in dd-mm-yyyy instead of default yyyy-mm-dd
- Every function: wrap risky logic (I/O, parsing, API/model calls) in try/except with specific, user-facing error messages — no bare `except:`
- Log errors before raising or displaying them
- After finishing a phase, run `/code-review` or `/simplify` and check output against this list
- when use st.caption use red color

# Answers & Plan
- keep your language for Answers & Plan short & simple as layman can understand, give small examples so user can understand better.